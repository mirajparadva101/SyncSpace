syncspace/
├── index.html                  # Landing + Auth + Hub + Dashboard
├── style.css                   # Glassmorphism + responsive + hub/presence UI
├── app.js                      # Full app logic (bugfix rewrite)
├── manifest.json               # PWA manifest
├── sw.js                       # Service Worker v2 (network-first shell)
├── schema.sql                  # Supabase schema + whiteboards + unique texts
├── vercel.json                 # Deploy config (safe SPA rewrite)
├── api/
│   └── config.js               # SUPABASE_URL / SUPABASE_ANON_KEY endpoint
├── .gitignore
├── README.md
└── file-structure.txt
